import React, {createClass, PropTypes} from "react";
import classNames from "./Login.less";

function Login({message = ""}) {
  return (
    <div>
      <h1>Login ...</h1>
    </div>
  );
}

Login.propTypes = {};

export default Login;
